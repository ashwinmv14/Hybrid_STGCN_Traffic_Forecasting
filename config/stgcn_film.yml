# STGCN + FiLM Only
# Tests the contribution of FiLM (temporal conditioning)

use_film: true
use_alignn: false
use_dynamic_adj: false

film_hidden: 64
alignn_hidden: 64
alpha_dyn: 0.005

# Training params
lr: 0.001
clip: 5.0
max_epochs: 100
patience: 30